-- Oracle DB에서 직접 실행 (ddl-auto: none 설정으로 자동 생성되지 않음)

CREATE TABLE MEMBERS (
    USER_ID   NUMBER GENERATED BY DEFAULT AS IDENTITY,
    NAME      VARCHAR2(100)   NOT NULL,
    BIRTHDATE DATE            NOT NULL,
    EMAIL     VARCHAR2(255)   NOT NULL,
    PASSWORD  VARCHAR2(255)   NOT NULL,
    USER_ROLE  VARCHAR2(20)   DEFAULT 'USER' NOT NULL,
    IS_DELETED NUMBER(1)      DEFAULT 0      NOT NULL,
    CONSTRAINT PK_MEMBERS PRIMARY KEY (USER_ID),
    CONSTRAINT UQ_MEMBERS_EMAIL UNIQUE (EMAIL)
);

-- 기존 테이블에서 변경할 경우 아래 DDL 사용
-- ALTER TABLE MEMBERS RENAME COLUMN ID TO USER_ID;
-- ALTER TABLE MEMBERS ADD (USER_ROLE VARCHAR2(20) DEFAULT 'USER' NOT NULL);
-- ALTER TABLE MEMBERS ADD (IS_DELETED NUMBER(1) DEFAULT 0 NOT NULL);
